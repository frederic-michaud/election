# Tout ce qu'il faut pour faire tourner le site.
# La base est SQLite (bibliothèque standard) : pas de pilote à installer.
Django==5.2.*
# Serveur d'application et service des fichiers statiques : avec DEBUG=0,
# Django refuse de servir /static/ lui-même (voir Dockerfile, compose.yaml).
gunicorn==23.*
whitenoise==6.*
plotly==6.*
geojson==3.*
python-dotenv==1.*

# Exigés par plotly.express, pas par notre code : px convertit ses entrées en
# DataFrame quoi qu'on lui passe. Réécrire les figures en plotly.graph_objects
# permettrait de s'en passer — ce sera le moment de la refonte graphique
# (PLAN_MODERNISATION.md, Partie 7), pas avant.
numpy==2.*
pandas==2.*
